// import {concat} from "../src/operators/concat";
// import {distinct} from "../src/operators/distinct";
// import {Enumerable, range, filter, take} from "../src/index";

// console.log(
//     Array.from(concat([1,2],[4,5]))
// );

// console.log(
//     Array.from(distinct([1,1,2,2,3,4]))
// );

// console.log(
//     Array.from(take(filter(range(1,Number.MAX_VALUE), x => x % 5 === 0), 5))
// );


